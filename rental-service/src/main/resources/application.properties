# Nome do aplicativo no Eureka
spring.application.name=rental-service

# Configurações do banco de dados MySQL para o rental-service
spring.datasource.url=jdbc:mysql://localhost:3306/locacao_veiculos
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuração do JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configurações do servidor (porta diferente para evitar conflito com outros serviços)
server.port=8083

# Configuração da chave secreta JWT (deve ser a mesma dos outros serviços)
jwt.secret=abcdefghijklmnopqrstuvwxyz1234567890

# Configurações do Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true
